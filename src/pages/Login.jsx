import React from 'react'

export default function Login() {
    return (
        <div>
            after login check if admin redirect to admin page else question page
            login
        </div>
    )
}
